spring:
  cloud:
    config:
      profile: @env@
      uri: http://localhost:9000
      username: hx
      password: hx
      discovery:
        enabled: true
        service-id: config-server
server:
  port: 2100
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka