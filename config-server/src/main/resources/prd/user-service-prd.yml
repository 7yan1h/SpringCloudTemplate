eureka:
  instance:
    # 采用ip注册
    prefer-ip-address: true
  client:
    # 注册自己
    register-with-eureka: true
    # 获取注册列表
    fetch-registry: true
    service-url:
      # 指定注册中心的地址
      defaultZone: http://localhost:1000/eureka