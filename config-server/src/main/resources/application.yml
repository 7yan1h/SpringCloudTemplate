spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          # 新建一个仓库,用来存放配置文件
          uri: https://github.com/ryan1h/SpringCloudTemplateConfig
          username: ryan1h
          password: hx1254188
          # 启动时克隆仓库
          clone-on-start: true
server:
  port: 9000