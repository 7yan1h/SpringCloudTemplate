spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/prd
  security:
    user:
      name: hx
      password: hx
server:
  port: 9100
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka